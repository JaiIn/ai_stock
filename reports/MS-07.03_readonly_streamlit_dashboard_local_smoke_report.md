# MS-07.03 Read-Only Streamlit Dashboard Local Smoke Report

## 수행 목적과 기준

- 목적: MS-07.02 최소 dashboard가 localhost에서 import, launch, HTTP 응답, safe render 가능한지 확인
- branch: `codex/ui/MS-07.03-readonly-streamlit-dashboard-local-smoke`
- base commit: `a45d6ec feat(ui): MS-07.02 add minimal read-only snapshot dashboard`
- 생성/수정 파일: 이 보고서, `docs/19_DETAILED_MICRO_WBS.md`, `references/endpoint_matrix.md`
- 기능 코드 수정: 없음
- commit/push: 없음

## local smoke 설계

- 기존 `app/streamlit_app.py`를 headless localhost server로 정확히 1회 실행
- HTTP root와 Streamlit health endpoint만 localhost에서 확인
- 브라우저 수동 확인이 불가능한 경우 Streamlit 공식 AppTest로 render tree 확인
- 대상 data source와 DB access는 기존 `local_snapshot_latest_read_model` 경계만 사용
- server smoke 종료 후 프로세스와 port를 닫고 DB metadata를 비교

## Streamlit 실행 결과

- 실행 명령: `python -m streamlit run app/streamlit_app.py --server.headless true --server.port 8501 --browser.gatherUsageStats false`
- Streamlit 실제 서버 실행 횟수: `1`
- server launch: 성공
- localhost port: `8501`
- root HTTP smoke: 수행, HTTP `200`
- health smoke: 수행, HTTP `200`, response `ok`
- server 종료: 성공
- 종료 후 port listener: `0`

PowerShell `Start-Process` 방식은 중복 `Path/PATH` 환경 이름 오류로 프로세스
생성 전에 실패했고 실행 횟수에 포함하지 않았습니다. 이후 독립 Python
프로세스로 서버를 한 번만 시작했습니다.

## 브라우저와 render 확인

- 브라우저 수동 UI 확인: 미수행
- 사유: in-app browser 연결이 workspace path URI metadata 오류로 실행 전에 차단됨
- 대체 확인: Streamlit 공식 `AppTest.from_file(...).run()` 성공
- render exception: `0`
- success message: `1`
- warning/error message: `0/0`

확인한 safe sections:

- dashboard title: `Local Snapshot Dashboard`
- data source
- selected symbol
- exchange pair
- DB open mode
- latest StockInfo
- latest PriceSnapshot
- latest Candle
- latest ExchangeRate
- completeness
- source counts
- read-only diagnostics

확인한 safe values:

- data source: `local_snapshot_latest_read_model`
- default DB path: `data/local/ai_stock.sqlite3`
- default symbol: `005930`
- default exchange pair: `USD/KRW`
- DB open mode: `readonly`
- completeness: 모든 component `Yes`
- source counts: stocks `1`, price snapshots `2`, candles `2`, exchange rates `2`

## 금지 UI와 sensitive field 확인

- live API/OAuth refresh action: 없음
- credential 입력란: 없음
- AI 추천 button 또는 문구: 없음
- 주문/계좌/자산/잔고/체결 button: 없음
- 실주문 button: 없음
- DB raw row 또는 raw response body 표시: 없음
- Access Token, credential, Authorization header, accountSeq 표시: 없음

위 항목은 AppTest element tree와 기존 source contract test 결과로 확인했습니다.

## DB read-only와 metadata

- read-only policy: SQLite URI `mode=ro`, `PRAGMA query_only=ON`
- DB path: `data/local/ai_stock.sqlite3`
- before size: `45056` bytes
- after size: `45056` bytes
- before modified time UTC: `2026-06-30T06:18:29.2574726Z`
- after modified time UTC: `2026-06-30T06:18:29.2574726Z`
- `actual_db_file_modified_this_stage=false`
- DB file Git tracked: `false`
- data directory Git tracked: `false`
- ignore rules: `.env.local`, `data/`, `*.sqlite`, `*.sqlite3`, `*.db`

## 안전 확인

- 실제 Toss API 호출: 없음
- OAuth token endpoint 호출: 없음
- live smoke 재실행: 없음
- fake smoke 재실행: 없음
- `.env.local` 읽기/출력/커밋: 없음
- Access Token 원문 출력/저장: 없음
- API KEY / SECRET KEY / Client ID / Client Secret 원문 출력/저장: 없음
- Authorization Bearer 원문 출력/저장: 없음
- credential/token/Auth header 원문 출력/저장: 없음
- accountSeq 요청/사용/저장: 없음
- 실제 주문/계좌/자산/잔고/체결 호출 또는 구현: 없음
- AI 추천 로직 구현: 없음
- 실주문 button 구현: 없음
- `pyproject.toml` 변경: 없음

## 검증 결과

사전 검증:

- `python -m compileall -q src tests app`: PASS
- `python -m unittest discover -s tests`: 260 tests PASS
- `python -m pytest`: 282 tests PASS
- `python scripts/dev_check.py`: PASS
- `ruff check src tests app`: PASS
- `git diff --check`: PASS

사후 검증도 동일 명령으로 재실행했으며 unittest `260`, pytest `282`를 포함해
모두 PASS했습니다.

AppTest 종료 시 OS 임시 디렉터리 cleanup permission warning이 발생했지만 render
결과와 repository 파일에는 영향이 없습니다. 기존 pytest cache warning도 비추적
임시 디렉터리 permission warning이며 검증 결과에 영향이 없습니다.

## 다음 단계 제안

MS-07.03 변경분은 별도 사용자 승인 전까지 커밋하거나 push하지 않습니다.
브라우저 연결 환경이 정상화된 경우에만 별도 승인 아래 수동 screenshot/layout
확인을 수행할 수 있습니다. API/OAuth, DB write, AI 추천, 실계좌 및 주문 기능은
계속 별도 범위로 유지합니다.
