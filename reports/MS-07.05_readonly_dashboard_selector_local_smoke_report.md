# MS-07.05 Read-Only Dashboard Selector Local Smoke Report

## 수행 목적과 기준

- 목적: MS-07.04 selector가 실제 local snapshot DB를 사용하는 Streamlit AppTest에서 안전하게 렌더링되고 validation되는지 확인
- branch: `codex/ui/MS-07.05-readonly-dashboard-selector-local-smoke`
- base commit: `c9e6f50 feat(ui): MS-07.04 add read-only dashboard selectors`
- 생성/수정 파일: 이 보고서, `docs/19_DETAILED_MICRO_WBS.md`, `references/endpoint_matrix.md`
- 기능 코드와 테스트 코드 수정: 없음
- commit/push: 없음

## Smoke 설계

- 실제 DB: `data/local/ai_stock.sqlite3`
- data source: `local_snapshot_latest_read_model`
- DB access: SQLite URI `mode=ro`, `PRAGMA query_only=ON`
- Streamlit AppTest session: `1`
- AppTest render run: `5`
- 시나리오: 기본값, valid custom pair, 빈 symbol, invalid currency, invalid symbol
- raw DB row 또는 파일 내용은 출력하지 않고 selector/status/section safe summary만 확인

## AppTest와 Streamlit 실행 결과

- AppTest 수행: 성공
- render exception: `0`
- Streamlit 서버 실행: 없음
- Streamlit 서버 실행 명령: 미실행
- Streamlit 서버 실행 횟수: `0`
- Streamlit 서버 기동 결과: 해당 없음
- HTTP smoke: 미수행
- 브라우저 수동 UI 확인: 미수행

서버·HTTP·브라우저 없이 AppTest만으로 selector widget과 render tree를 확인했습니다.

## Selector 렌더와 기본값

- text input count: `3`
- symbol input: 렌더 성공
- base currency input: 렌더 성공
- quote currency input: 렌더 성공
- default symbol: `005930`
- default base currency: `USD`
- default quote currency: `KRW`
- default exchange pair: `USD/KRW`
- data source 표시: `local_snapshot_latest_read_model`
- DB mode 표시: `readonly`

## Selector validation 결과

- valid selector `005930`, `eur`, `usd`: `005930`, `EUR/USD`로 전달
- local DB에 EUR/USD rate가 없어 기존 completeness warning으로 안전하게 표시
- 빈 symbol: 기본값 `005930`으로 fallback
- lowercase `usd`/`krw`: `USD/KRW`로 정규화
- invalid currency `US`: ASCII 3자리 validation warning
- invalid symbol `unsafe symbol`: 1~32자 안전 문자 validation warning
- 모든 validation 시나리오에서 render exception: `0`
- validation failure에서 API/OAuth 호출, DB write, schema initialize, migration: 없음

## 확인한 safe sections와 금지 control

확인한 sections:

- Local read-only selection
- Data source와 selected symbol/pair
- StockInfo, PriceSnapshot, Candle, ExchangeRate
- Completeness
- Source counts
- Read-only diagnostics

금지 control 확인:

- button count: `0`
- API refresh/OAuth/live refresh button: 없음
- credential/API key/secret/token input: 없음
- accountSeq input: 없음
- AI 추천 button: 없음
- 주문/계좌/자산/잔고/체결 button: 없음
- DB write/migration/schema initialize control: 없음

## DB file metadata와 Git 안전

- DB exists: `true`
- before size: `45056` bytes
- after size: `45056` bytes
- before modified time UTC: `2026-06-30T06:18:29.2574726Z`
- after modified time UTC: `2026-06-30T06:18:29.2574726Z`
- `actual_db_file_modified_this_stage=false`
- DB file Git tracked: `false`
- data directory Git tracked: `false`
- `.gitignore`: `.env.local`, `data/`, `*.sqlite`, `*.sqlite3`, `*.db` 유지
- DB row 전문과 DB 파일 내용 출력: 없음

## 금지 작업과 민감정보 확인

- 실제 Toss API 호출: 없음
- OAuth token endpoint 호출: 없음
- live smoke 재실행: 없음
- fake smoke 재실행: 없음
- `.env.local` 사용·출력·커밋: 없음
- credential, Access Token, Authorization header 원문 출력·저장: 없음
- accountSeq 요청·사용·저장: 없음
- 주문/계좌/자산/잔고/체결 호출 또는 구현: 없음
- AI 추천 로직과 실주문 button 구현: 없음
- `pyproject.toml` 변경: 없음

## 검증과 warning

- 사전/사후 compileall, unittest, pytest, dev_check, ruff, diff check: PASS
- unittest: `264` tests PASS
- pytest: `286` tests PASS
- AppTest 종료 시 OS 임시 디렉터리 cleanup permission warning이 발생했으나 render, repository, DB 안전성에 영향 없음
- pytest cache warning은 비추적 임시 디렉터리 permission warning이며 검증 결과에 영향 없음
- LF/CRLF 안내는 Git/Windows 줄바꿈 관련 안내이며 기능, 테스트, read-only 정책, DB 안전성에 영향 없는 비차단 안내

## 다음 단계 제안

MS-07.05 변경분은 사용자 승인 전까지 커밋하거나 push하지 않습니다. 후속 단계도
API/OAuth, DB write, AI 추천, 실계좌 및 주문 기능과 분리해야 합니다.
