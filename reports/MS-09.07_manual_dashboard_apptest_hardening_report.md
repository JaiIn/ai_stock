# MS-09.07 Manual Dashboard AppTest Hardening Report

## Purpose

MS-09.07 hardens the MS-09.06 observation-only manual/watchlist preflight UI
with additional Streamlit AppTest coverage. This checkpoint does not add new UI
features or data paths. It fixes expectations for fixture scenario coverage,
safe copy, safety badges, warnings, rows, forbidden field sanitization, and
forbidden button/action/input absence.

## Branch

```text
codex/ui/MS-09.07-manual-dashboard-apptest-hardening
```

## Base Commit

```text
33d424e feat(ui): MS-09.06 add manual dashboard UI integration
```

## Created/Modified Files

```text
tests/test_ai_recommendation_manual_dashboard_apptest_hardening.py
docs/19_DETAILED_MICRO_WBS.md
references/endpoint_matrix.md
reports/MS-09.07_manual_dashboard_apptest_hardening_report.md
```

## AppTest Hardening Summary

The new AppTest suite renders the existing Streamlit app without starting a
server, making HTTP requests, or opening a browser. It selects each in-memory
fixture scenario exposed by the existing `Fixture scenario` selectbox and
verifies safe display output for valid, invalid, duplicate, disabled,
insufficient-data, forbidden-field sanitized, and empty watchlist states.

## `app/streamlit_app.py` Modification Status

```text
unchanged
```

No app code changes were needed. The existing MS-09.06 section labels and
rendered copy were stable enough for AppTest hardening.

## Verified UI Sections

```text
Manual Watchlist Dashboard Preflight
Fixture scenario selectbox
Manual preflight safety badges
Manual preflight warnings
Manual preflight rows
Fixture scenario coverage
Summary metrics
Next action hint
```

## Verified Fixture Scenarios

```text
Fixture Basic Manual Symbols
Fixture Mixed Symbols
Fixture Duplicates And Disabled
Fixture Insufficient Data Review
Fixture Forbidden Fields Sanitized
Fixture Empty Watchlist
```

## Scenario Selectbox Verification

The hardening test verifies that the selectbox options exactly match the six
fixture scenario display names and that selecting each option re-renders the
expected safe watchlist status, row status, warning, or empty-state text.

## Fixture Coverage Expander Verification

The fixture coverage expander is verified to list all six fixture scenarios and
their safe status coverage, including duplicate, disabled, insufficient-data,
and empty-watchlist statuses.

## Observation-Only Copy Verification

The rendered app is checked for:

```text
observation-only
manual-or-fixture watchlist
not investment advice
observation and validation only
not an investment recommendation
there is no order function
Toss API/account/OAuth/LLM/DB write not used
in-memory fixture/manual preflight
```

## Safety Badge Verification

The hardening test verifies visible badges:

```text
observation_only
mock_or_manual_input_only
no_real_order
no_account_access
no_live_api
no_llm
no_db_write
insufficient_data
```

## Warning and Diagnostics Display Verification

The hardening test verifies safe warning output for:

```text
invalid_candidates_need_review
duplicate_candidates_need_review
disabled_candidates_not_selectable
insufficient_data_review
forbidden_fields_reported_in_diagnostics_only
```

Forbidden field diagnostics are expected to remain generic in the rendered UI.
Actual forbidden values are not displayed.

## Row/Table Display Verification

The hardening test verifies manual preflight rows render allowed fields such as
symbol, validation status, enabled, selectable, source, source label, tags,
group, priority, note, and warnings. It does not allow account, credential,
order, target price, expected return, score, or action fields.

## Forbidden Button/Action/Input Absence

The hardening test confirms the absence of:

```text
Buy/Sell/Hold buttons or recommendation actions
Strong buy / target price / profit expected / must buy / must sell actions
Order button / real order button / submit order controls
API refresh / live API refresh controls
OAuth login controls
Credential / API key / secret key / token inputs
accountSeq input
File upload / file path input
DB refresh controls
Raw API response panel
Raw DB row panel
```

## Safety Confirmations

- Actual recommendation implementation: not implemented.
- Scoring implementation: not implemented.
- Buy/sell/hold judgment: not implemented.
- Watchlist persistence: not implemented.
- Watchlist file loader: not implemented.
- Fixture file loader: not implemented.
- Toss API call: not performed.
- OAuth token endpoint call: not performed.
- LLM/OpenAI call: not performed.
- New DB read implementation: not added.
- DB write: not performed.
- File read implementation: not added.
- File write implementation: not added.
- credential_required=false confirmed through rendered preflight policy.
- accountSeq_required=false confirmed through rendered preflight policy.
- real_order_required=false confirmed through rendered preflight policy.
- Streamlit server: not run.
- HTTP smoke: not run.
- Manual browser run: not performed.

## Sensitive Information Handling

`.env.local` was not read, printed, or committed. No actual API key, secret key,
client secret, OpenAI API key, access token, Authorization Bearer value,
accountSeq value, raw API response body, raw request body, raw DB row, or DB
file content was added. Policy terms appear only in tests, documentation, and
reports.

## DB and Data Handling

The AppTest suite records `data/local/ai_stock.sqlite3` metadata if the file
exists and verifies size and modified time are unchanged after render. The DB
file contents are not printed. The DB file and `data/` directory remain
untracked/ignored by Git.

## Unchanged Scope Verification

```text
src/ai_stock/clients: unchanged
src/ai_stock/storage: unchanged
src/ai_stock/paper_trading: unchanged
src/ai_stock/risk/paper_trading.py: unchanged
src/ai_stock/recommendation/scoring.py: unchanged
src/ai_stock/recommendation/dashboard_preflight.py: unchanged
src/ai_stock/recommendation/watchlist_fixtures.py: unchanged
src/ai_stock/recommendation/watchlist_source.py: unchanged
src/ai_stock/recommendation/watchlist_model.py: unchanged
src/ai_stock/recommendation/candidate_input_preflight.py: unchanged
src/ai_stock/recommendation/__init__.py: unchanged
README.md: unchanged
pyproject.toml: unchanged
docs/28_READONLY_DASHBOARD_RUNBOOK.md: unchanged
```

## Verification Results

```text
python -m compileall -q src tests app
PASS

python -m unittest discover -s tests
PASS - Ran 448 tests in 9.372s; OK

python -m pytest
PASS - 470 passed, 1 warning

python scripts/dev_check.py
PASS - compileall, unittest, pytest, and git diff checks completed

ruff check src tests app
PASS - All checks passed

git diff --check
PASS - no whitespace errors; LF/CRLF advisory warnings only

git status --short
Expected MS-09.07 changed files only, with existing pytest-cache-files
permission warnings observed by Git status.
```

Targeted AppTest hardening check:

```text
python -m pytest tests/test_ai_recommendation_manual_dashboard_apptest_hardening.py
PASS - 7 passed, 1 warning
```

## Known Warnings

- The initial bare `python` command can fail on this Windows environment due to
  the Microsoft Store execution alias; the project virtualenv Python is used
  for verification.
- Pytest cache path warnings for restricted `pytest-cache-files-*` directories
  may appear on Windows.
- Streamlit bare-mode `missing ScriptRunContext` warnings may appear during
  AppTest or unittest runs.
- Python temporary directory cleanup can emit `PermissionError` after
  successful tests on Windows.
- Git may report LF-to-CRLF advisory warnings for touched text files.

## Next Step

```text
MS-10.00 feature/data quality model preflight
```
