# MS-07.07 Read-Only Dashboard Runbook Report

## 수행 목적과 기준

- 수행 목적: 사용자가 MS-07.02~MS-07.06 read-only Streamlit dashboard를 로컬에서 안전하게 실행·확인·종료할 수 있도록 runbook과 체크리스트 문서화
- 생성 브랜치: `codex/ui/MS-07.07-readonly-dashboard-runbook`
- 기준 커밋: `77825de test(ui): MS-07.06 verify read-only dashboard selector server smoke`
- 생성/수정 파일:
  - `docs/28_READONLY_DASHBOARD_RUNBOOK.md`
  - `docs/19_DETAILED_MICRO_WBS.md`
  - `references/endpoint_matrix.md`
  - `reports/MS-07.07_readonly_dashboard_runbook_report.md`
- `README.md` 수정: 없음
- 기능 코드, 테스트 코드, 실행 스크립트 수정 또는 추가: 없음
- commit/push: 미수행

## Runbook 작성 요약

`docs/28_READONLY_DASHBOARD_RUNBOOK.md`에 다음 내용을 문서화했습니다.

- local-only read-only dashboard의 목적과 지원 범위
- data source `local_snapshot_latest_read_model`
- default DB path `data/local/ai_stock.sqlite3`
- default symbol `005930`, default pair `USD/KRW`
- symbol/base/quote selector의 trim, uppercase, validation 및 local query 전용 정책
- 실행 전 Git, Python 환경, DB 존재 여부, safe metadata, Git 미추적 체크리스트
- 정확한 Streamlit 실행 명령과 `Ctrl+C` 종료 방법
- 종료 후 포트 `8501` listener 정리 확인
- dashboard title, selector, four data sections, completeness, source counts,
  read-only diagnostics 화면 확인 체크리스트
- missing DB와 partial data의 safe warning 정책
- 금지 UI/기능과 민감정보 정책
- SQLite URI `mode=ro`, `PRAGMA query_only=ON`, no write/init/migration 정책
- pytest cache, LF/CRLF, workspace URI metadata known non-blocking warnings
- DB 부재, 포트 충돌, invalid selector, partial data, 브라우저 미기동 troubleshooting
- live API/OAuth/AI/계좌/주문 기능은 별도 Micro Stage와 승인 필요

## 실행과 운영 절차 문서화

- 실행 명령 문서화: 완료
  - `python -m streamlit run app/streamlit_app.py --server.headless true --server.port 8501 --browser.gatherUsageStats false`
- 종료 방법 문서화: 완료
  - 실행 터미널에서 `Ctrl+C`
  - 종료 후 포트 `8501` listener가 없어야 함
  - 소유 프로세스를 확인한 뒤 자신이 실행한 Streamlit 프로세스만 종료
- selector 사용법 문서화: 완료
- 실행 전 체크리스트 문서화: 완료
- 화면 확인 체크리스트 문서화: 완료
- troubleshooting 문서화: 완료

## 안전 정책 문서화

- 금지 UI/기능 문서화: 완료
- 민감정보 정책 문서화: 완료
- DB 안전 정책 문서화: 완료
- known non-blocking warnings 문서화: 완료
- API KEY / SECRET KEY 불필요 명시: 완료
- Client ID / Client Secret 불필요 명시: 완료
- Access Token 불필요 명시: 완료
- `.env.local` 불필요 명시: 완료
- accountSeq 불필요 명시: 완료
- Authorization Bearer 원문 출력·저장 금지: 명시
- raw response body 및 DB row 전문 출력·저장 금지: 명시

## 이번 단계 실행 및 변경 범위 확인

- 실제 Toss API 호출: 없음
- OAuth token endpoint 호출: 없음
- live smoke 재실행: 없음
- fake smoke 재실행: 없음
- Streamlit 서버 실행: 없음
- AppTest 실행: 없음
- HTTP smoke 실행: 없음
- 브라우저 수동 UI 확인: 없음
- `.env.local` 사용·출력·커밋: 없음
- credential, Access Token, Authorization header 원문 출력·저장: 없음
- API KEY / SECRET KEY / Client ID / Client Secret 원문 출력·저장: 없음
- accountSeq 요청·사용·저장: 없음
- 실제 주문/계좌/자산/잔고/체결 호출 또는 구현: 없음
- AI 추천 로직 구현: 없음
- 실주문 button 구현: 없음
- DB file 내용 또는 row 전문 출력: 없음
- `data/local/ai_stock.sqlite3` 수정: 없음
- DB file Git tracked: `false`
- data directory Git tracked: `false`
- `.gitignore` SQLite/data 규칙 유지: 확인
- `pyproject.toml` 변경: 없음
- `app/streamlit_app.py` 변경: 없음
- `src/` 변경: 없음
- `tests/` 변경: 없음
- `README.md` 변경: 없음

## 검증 및 경고

- compileall: PASS
- unittest: PASS
- pytest: PASS
- dev_check: PASS
- ruff: PASS
- `git diff --check`: PASS
- pytest cache warning은 비추적 임시 디렉터리 permission warning이며 검증 결과에 영향 없음
- LF/CRLF 안내는 Git/Windows 줄바꿈 관련 비차단 안내이며 기능, 테스트, read-only 정책, DB 안전성에 영향 없음

## 다음 단계 제안

MS-07.07 문서 변경은 사용자 승인 전까지 commit 또는 push하지 않습니다. 후속
Micro Stage에서도 live API/OAuth, AI 추천, 계좌 및 주문 기능을 현재 local-only
read-only dashboard와 분리해야 합니다.
