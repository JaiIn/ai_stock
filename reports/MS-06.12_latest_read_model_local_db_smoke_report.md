# MS-06.12 Latest Read Model Local DB Smoke Report

## 수행 목적과 기준

- 목적: MS-06.11 latest snapshot read model이 fixture뿐 아니라 실제 local SQLite DB에서도 안전하게 동작하는지 read-only로 확인
- branch: `codex/data/MS-06.12-latest-read-model-local-db-smoke`
- base commit: `78585c9 feat(data): MS-06.11 add local snapshot latest read model`
- smoke phase: `local_snapshot_latest_read_model_db_smoke`
- underlying read model phase: `local_snapshot_latest_read_model`

## 생성/수정 파일

- `docs/19_DETAILED_MICRO_WBS.md`
- `references/endpoint_matrix.md`
- `reports/MS-06.12_latest_read_model_local_db_smoke_report.md`

CLI와 read model은 실제 DB schema에 맞아 코드 수정이 필요하지 않았습니다.

## 실행 대상과 read-only 설계

- DB path: `data/local/ai_stock.sqlite3`
- default symbol: `005930`
- default exchange pair: `USD/KRW`
- default candle interval: `1d`
- DB open mode: SQLite URI `mode=ro`
- `PRAGMA query_only = ON`: 사용
- schema initialize 및 write SQL: 없음
- latest read model CLI 실행 횟수: 정확히 `1`

## 사전 검증

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS, 241 tests
- `python -m pytest`: PASS, 263 tests
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

모든 사전 검증이 통과한 뒤에만 실제 local DB 대상 CLI를 실행했습니다.

## 실행 결과

- success: `true`
- safe error: 없음
- source counts: stocks 1, price snapshots 2, candles 2, exchange rates 2
- StockInfo completeness: `true`
- PriceSnapshot completeness: `true`
- Candle completeness: `true`
- ExchangeRate completeness: `true`
- overall completeness: `true`
- selected timestamps present: `true`
- Decimal values preserved internally: `true`
- Decimal/timestamp JSON string-safe conversion: `true`
- currency present: `true`
- OHLCV present: `true`
- StockWarnings included: `false`
- `stock_warnings_deferred=true`
- `db_write_allowed_this_stage=false`
- `actual_db_file_modified_this_stage=false`

DB row 전문과 DB 파일 내용은 출력하지 않았으며, 위 결과는 승인된 safe summary와 completeness 정보만 기록합니다.

## DB file safe metadata before/after

| Metadata | Before | After | Result |
| --- | --- | --- | --- |
| size | 45056 bytes | 45056 bytes | unchanged |
| modified time UTC | `2026-06-30T06:18:29.2574726Z` | `2026-06-30T06:18:29.2574726Z` | unchanged |

- DB file modified: `false`
- DB file Git tracked: `false`
- data directory Git tracked: `false`
- SQLite file ignored by Git: `true`
- ignore rules: `data/`, `*.sqlite`, `*.sqlite3`, `*.db`, `.env.local`

## 안전 제한 확인

- 실제 API 호출: 없음
- OAuth token endpoint 호출: 없음
- live smoke 재실행: 없음
- fake smoke 재실행: 없음
- `.env.local` 읽기/출력/커밋: 없음
- Access Token 원문 출력/저장: 없음
- API KEY / SECRET KEY / Client ID / Client Secret 원문 출력/저장: 없음
- Authorization Bearer 원문 출력/저장: 없음
- credential/token/Auth header 저장: 없음
- accountSeq 요청/사용/저장: 없음
- 실제 주문/계좌/자산/잔고/체결 호출 또는 구현: 없음
- request body 및 raw response body 저장: 없음
- `pyproject.toml` 변경: 없음

## 사후 검증

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS
- `python -m pytest`: PASS
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS
- pytest cache warning은 비추적 임시 디렉터리 permission warning이며 검증 결과에 영향 없음

## 다음 단계

MS-06.12 변경분은 별도 사용자 승인 전까지 커밋하거나 push하지 않습니다. UI, 리포트 또는 AI 추천 입력 연계는 별도 Micro Stage와 승인 범위에서 수행합니다.
