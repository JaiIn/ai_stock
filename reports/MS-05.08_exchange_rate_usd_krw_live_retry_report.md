# MS-05.08 Exchange Rate USD/KRW Live Retry Report

## 수행 목적

MS-05.07에서 공식 OpenAPI 기준으로 정렬한 exchange-rate query schema를 적용하여
`baseCurrency=USD`, `quoteCurrency=KRW`를 명시한 read-only live smoke test를
승인된 범위에서 정확히 한 번 수행했습니다.

## 생성 브랜치

- `codex/api/MS-05.08-exchange-rate-usd-krw-live-retry`

## 기준 커밋

- `422faf1 fix(toss-api): MS-05.07 realign exchange rate schema`

## 사전 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS, 115 tests
- `python -m pytest`: PASS, 137 tests
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

## env key 존재 확인 결과

값 원문은 출력하지 않고 키 존재와 안전 설정 일치 여부만 확인했습니다.

- `TOSS_CLIENT_ID`: 존재
- `TOSS_CLIENT_SECRET`: 존재
- `ALLOW_LIVE_API=true`: 확인
- `ALLOW_REAL_ORDER=false`: 확인
- `DRY_RUN_ONLY=true`: 확인

## `.env.local` Git 제외 확인 결과

- 로컬 파일 존재: 확인
- Git tracked: 아니오
- Git status 표시: 없음
- `.gitignore` 제외: 확인
- 파일 내용 출력: 없음

## 실제 호출 범위

- `POST /oauth2/token`: 1회
- `GET /api/v1/exchange-rate?baseCurrency=USD&quoteCurrency=KRW`: 1회
- 추가 재시도: 없음
- 그 외 실제 Toss 업무 API 호출: 없음

## OAuth token endpoint 호출 결과

- 호출 여부: 수행
- 호출 횟수: 1회
- 성공 여부: 성공
- `token_type`: `Bearer`
- `expires_in`: `86399`
- Access Token 원문 출력 또는 저장: 없음

## Exchange Rate 호출 결과

- 호출 여부: 수행
- 호출 횟수: 1회
- 사용 query: `baseCurrency=USD`, `quoteCurrency=KRW`
- `dateTime`: 생략
- live smoke test 결과: 성공
- `success`: `true`
- `phase`: `readonly_exchange_rate`
- endpoint: `GET /api/v1/exchange-rate`
- HTTP status code: `200`
- safe error code/type: 없음
- safe error message: 없음

## parsed response schema 요약

- `baseCurrency`: `USD`
- `quoteCurrency`: `KRW`
- `rate`: 존재
- `midRate`: 존재
- `basisPoint`: 존재
- `rateChangeType`: 존재
- `validFrom`: 존재
- `validUntil`: 존재
- raw response body 전문 저장: 없음

## 민감정보 안전 확인

- Access Token 원문 미출력/미저장
- API KEY / SECRET KEY / Client ID / Client Secret 원문 미출력/미저장
- Authorization Bearer 원문 미출력/미저장
- request body 원문 미출력/미저장
- `.env.local` 내용 미출력/미커밋
- raw response body 전문 미저장

## 범위 제한 확인

- accountSeq 요청/사용/저장 없음
- `GET /api/v1/exchange-rate` 외 실제 업무 API 호출 없음
- 실제 주문/계좌/자산/잔고/체결 호출 없음
- retry loop 및 자동 재시도 없음
- DB 저장 없음
- 실제 DB 파일 미포함
- `pyproject.toml` 변경 없음

## 다음 단계 제안

이번 성공 결과와 문서 변경을 검토한 뒤 별도 승인으로 커밋합니다. 추가 live API
호출이나 호출 범위 확대는 별도 Micro Stage와 사용자 승인이 있기 전까지 수행하지
않습니다.
