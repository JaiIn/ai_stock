# MS-05.13 Prices Single-symbol Live Smoke Report

## 수행 목적

MS-05.12에서 공식 schema에 맞춰 정렬한 Prices API를 기준으로
`GET /api/v1/prices?symbols=005930` live smoke test를 정확히 1회 수행했습니다.
이번 단계는 read-only 가격 조회 smoke test이며 주문, 계좌, 자산, 잔고, 체결 API를
호출하지 않았습니다.

## 생성 브랜치

- `codex/api/MS-05.13-prices-single-symbol-live-smoke`

## 기준 커밋

- `7c1c867 feat(toss-api): MS-05.12 align Prices read-only schema`

## 사전 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS
- `python -m pytest`: PASS, 170 tests
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

## env key 존재 확인 결과

값은 출력하지 않고 키 존재와 안전 플래그만 확인했습니다.

- `TOSS_CLIENT_ID`: present
- `TOSS_CLIENT_SECRET`: present
- `ALLOW_LIVE_API=true`: present
- `ALLOW_REAL_ORDER=false`: present
- `DRY_RUN_ONLY=true`: present

## .env.local Git 제외 확인 결과

- `.env.local` 존재 여부: 존재 확인
- `git status --short -- .env.local`: 출력 없음
- `git ls-files -- .env.local`: 출력 없음
- `git check-ignore .env.local`: ignored
- `.env.local` 내용 출력: 없음

## 실제 호출 범위

이번 단계에서 수행한 실제 네트워크 호출은 아래 2개뿐입니다.

1. `POST /oauth2/token`: 1회
2. `GET /api/v1/prices?symbols=005930`: 1회

추가 retry loop 또는 자동 재시도는 수행하지 않았습니다.

## OAuth token endpoint 호출 여부와 호출 횟수

- 호출 여부: 수행
- 호출 횟수: 1회

## GET /api/v1/prices 호출 여부와 호출 횟수

- 호출 여부: 수행
- 호출 횟수: 1회
- 사용 query: `symbols=005930`

## live smoke test 성공/실패

- 결과: 성공
- phase: `readonly_prices`
- HTTP status code: `200`
- safe error summary: 없음

## parsed response schema 요약

raw response body 전문은 저장하지 않았고 safe diagnostics와 parsed field 존재 여부만
기록했습니다.

- requested symbols: `005930`
- parsed result count: `1`
- parsed symbol 존재 여부: 확인
- parsed lastPrice 존재 여부: 확인
- parsed currency 존재 여부: 확인
- parsed timestamp 처리 여부: 확인
- Decimal 변환 성공 여부: 확인
- token_type: `Bearer`
- expires_in: `86399`

## 민감정보/원문 미출력 및 미저장 확인

- Access Token 원문 출력/저장: 없음
- credential 원문 출력/저장: 없음
- Client ID / Client Secret 원문 출력/저장: 없음
- Authorization Bearer 원문 출력/저장: 없음
- request body 원문 출력/저장: 없음
- raw response body 전문 저장: 없음
- `.env.local` 내용 출력/커밋: 없음

## accountSeq 요청 없음 확인

- accountSeq 요청/사용/저장: 없음
- `X-Tossinvest-Account` header 사용: 없음

## 다른 업무 API 호출 없음 확인

- `GET /api/v1/prices` 외 실제 업무 API 호출: 없음
- `GET /api/v1/stocks` 호출: 없음
- `GET /api/v1/stocks/{symbol}/warnings` 호출: 없음
- `GET /api/v1/candles` 호출: 없음
- 주문/계좌/자산/잔고/체결 API 호출 또는 구현: 없음

## 생성/수정 파일

- `scripts/prices_smoke_test.py`
- `src/ai_stock/clients/prices_smoke.py`
- `src/ai_stock/clients/__init__.py`
- `tests/test_prices_live_smoke.py`
- `references/endpoint_matrix.md`
- `reports/MS-05.13_prices_single_symbol_live_smoke_report.md`

## 사후 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS
- `python -m pytest`: PASS
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS
- `git status`: MS-05.13 working-tree changes only
- pytest cache warning: 비추적 임시 cache directory permission warning이며 검증 결과에 영향 없음

## 실제 DB 파일 미포함 확인

- tracked `data/` directory: 없음
- tracked `*.sqlite`, `*.sqlite3`, `*.db`: 없음
- 실제 DB 저장 로직 추가: 없음

## pyproject.toml 변경 없음 확인

- `pyproject.toml` 변경: 없음

## 다음 단계 제안

MS-05.13 변경분 검토 후 별도 승인에 따라 커밋을 진행합니다. 다음 read-only 후보는
별도 Micro Stage에서 schema preflight를 먼저 수행한 뒤 승인된 live smoke만 실행합니다.
