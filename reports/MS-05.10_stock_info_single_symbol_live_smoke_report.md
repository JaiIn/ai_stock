# MS-05.10 Stock Info Single-symbol Live Smoke Report

## 수행 목적

MS-05.09에서 공식 schema에 정렬한 Stock Info client/parser를 사용하여 단일
공식 예시 symbol `005930`에 대한 read-only live smoke test를 승인된 범위에서
정확히 한 번 수행했습니다.

## 생성 브랜치

- `codex/api/MS-05.10-stock-info-single-symbol-live-smoke`

## 기준 커밋

- `0b43c34 feat(toss-api): MS-05.09 align Stock Info read-only schemas`

## 생성/수정 파일

- `src/ai_stock/clients/stock_info_smoke.py`
- `src/ai_stock/clients/__init__.py`
- `scripts/stock_info_smoke_test.py`
- `tests/test_stock_info_live_smoke.py`
- `docs/19_DETAILED_MICRO_WBS.md`
- `references/endpoint_matrix.md`
- `reports/MS-05.10_stock_info_single_symbol_live_smoke_report.md`

## 사전 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS, 129 tests
- `python -m pytest`: PASS, 151 tests
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

## env key 존재 확인 결과

값 원문은 출력하지 않고 키 존재와 안전 설정 일치 여부만 확인했습니다.

- `TOSS_CLIENT_ID`: 존재
- `TOSS_CLIENT_SECRET`: 존재
- `ALLOW_LIVE_API=true`: 확인
- `ALLOW_REAL_ORDER=false`: 확인
- `DRY_RUN_ONLY=true`: 확인

## `.env.local` Git 제외 확인 결과

- 로컬 파일 존재: 확인
- Git tracked: 아니오
- Git status 표시: 없음
- `.gitignore` 제외: 확인
- 파일 내용 출력: 없음

## 실제 호출 범위

- `POST /oauth2/token`: 1회
- `GET /api/v1/stocks?symbols=005930`: 1회
- 추가 재시도: 없음
- `GET /api/v1/stocks/{symbol}/warnings`: 호출 없음
- 그 외 실제 Toss 업무 API 호출: 없음

## Live API Safety Gate

호출 전에 아래 metadata로 Safety Gate를 통과했습니다.

- method: `GET`
- path: `/api/v1/stocks`
- endpoint category: `stock_info`
- requires auth: true
- requires accountSeq: false
- read-only: true

## live smoke 결과

- 결과: 성공
- `success`: `true`
- `phase`: `readonly_stock_info`
- endpoint: `GET /api/v1/stocks`
- HTTP status code: `200`
- requested symbols: `005930`
- parsed result count: `1`
- safe error code/type: 없음
- safe error message: 없음

## parsed response schema 요약

- 요청 symbol `005930` 존재: 확인
- `symbol`: 존재
- `name`: 존재
- `market`: 존재
- `currency`: 존재
- `sharesOutstanding`: 존재
- optional field 처리 경로: 확인
- raw response body 전문 저장: 없음

## 민감정보 안전 확인

- Access Token 원문 미출력/미저장
- API KEY / SECRET KEY / Client ID / Client Secret 원문 미출력/미저장
- Authorization Bearer 원문 미출력/미저장
- request body 원문 미출력/미저장
- `.env.local` 내용 미출력/미커밋
- raw response body 전문 미저장

## 범위 제한 확인

- accountSeq 요청/사용/저장 없음
- `GET /api/v1/stocks` 외 실제 업무 API 호출 없음
- `GET /api/v1/stocks/{symbol}/warnings` 호출 없음
- 실제 주문/계좌/자산/잔고/체결 호출 없음
- retry loop 및 자동 재시도 없음
- DB 저장 없음
- 실제 DB 파일 미포함
- `pyproject.toml` 변경 없음

## 다음 단계 제안

이번 성공 결과와 코드·문서 변경을 검토한 뒤 별도 승인으로 커밋합니다. warnings
endpoint 또는 다른 read-only API의 live 호출은 별도 Micro Stage와 사용자 승인
전까지 수행하지 않습니다.

## 사용자 승인 요청

MS-05.10 Stock Info single-symbol live smoke 결과를 검토한 뒤 변경분 커밋 여부를
승인해 주세요.
