# MS-05.15 Candles Single-symbol Live Smoke Report

## 수행 목적

MS-05.14에서 공식 schema에 맞춰 정렬한 Candles read-only endpoint를
대상으로, 승인된 단일 live smoke query를 정확히 1회 실행했습니다.

## 생성 브랜치

`codex/api/MS-05.15-candles-single-symbol-live-smoke`

## 기준 커밋

- `ead8da7 feat(toss-api): MS-05.14 align Candles read-only schema`

## 사전 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS
- `python -m pytest`: PASS
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

## env key 존재 확인 결과

값은 출력하지 않았고 존재 여부와 안전 플래그 조건만 확인했습니다.

- `TOSS_CLIENT_ID`: present
- `TOSS_CLIENT_SECRET`: present
- `ALLOW_LIVE_API=true`: satisfied
- `ALLOW_REAL_ORDER=false`: satisfied
- `DRY_RUN_ONLY=true`: satisfied

## .env.local Git 제외 확인 결과

- `.env.local` local existence: yes
- `.gitignore` exclude rule: present
- `git status -- .env.local`: not listed
- `git ls-files -- .env.local`: not listed
- `.env.local` contents: not printed

## 실제 호출 범위

허용된 실제 네트워크 호출만 수행했습니다.

1. `POST /oauth2/token`: 1회
2. `GET /api/v1/candles?symbol=005930&interval=1d&count=1&adjusted=true`: 1회

No retry loop or automatic retry was used.

## OAuth token endpoint 호출 여부와 호출 횟수

- 호출 여부: yes
- 호출 횟수: 1
- Access Token 원문: 미출력/미저장

## GET /api/v1/candles 호출 여부와 호출 횟수

- 호출 여부: yes
- 호출 횟수: 1
- endpoint: `GET /api/v1/candles`
- query: `symbol=005930&interval=1d&count=1&adjusted=true`
- `before` parameter: not used

## Live API Safety Gate

호출 전 다음 metadata 기준으로 Safety Gate를 통과했습니다.

- method: `GET`
- path: `/api/v1/candles`
- endpoint_category: `market_data`
- requires_auth: true
- requires_account_seq: false
- is_read_only: true

## live smoke test 성공/실패

- result: success
- phase: `readonly_candles`
- HTTP status code: `200`

## safe error summary

성공 응답이므로 safe error는 없습니다.

- safe_error_code: none
- safe_error_type: none
- safe_error_message: none

## parsed response schema 요약

Raw response body 전문은 저장하지 않았고, parser 결과의 안전한 존재 여부만
기록했습니다.

- requested symbol: `005930`
- requested interval: `1d`
- requested count: `1`
- requested adjusted: `true`
- parsed candles count: `1`
- parsed candle timestamp 존재 여부: yes
- parsed open/high/low/close 존재 여부: yes
- parsed volume 존재 여부: yes
- parsed currency 존재 여부: yes
- parsed nextBefore 처리 여부: yes
- Decimal 변환 성공 여부: yes

## 민감정보 보호 확인

- Access Token 원문 미출력/미저장: yes
- credential 원문 미출력/미저장: yes
- Authorization Bearer 원문 미출력/미저장: yes
- Client ID 원문 미출력/미저장: yes
- Client Secret 원문 미출력/미저장: yes
- request body 원문 미출력/미저장: yes
- raw response body 전문 미저장: yes

## 호출하지 않은 endpoint 확인

- accountSeq 요청/사용/저장: 없음
- `GET /api/v1/candles` 외 실제 업무 API 호출: 없음
- `GET /api/v1/prices` 호출: 없음
- `GET /api/v1/stocks` 호출: 없음
- `GET /api/v1/stocks/{symbol}/warnings` 호출: 없음
- 주문/계좌/자산/잔고/체결 endpoint 호출: 없음

## 실제 DB 파일 미포함 확인

- `data/` Git 추적: 없음
- `*.sqlite`, `*.sqlite3`, `*.db` Git 추적: 없음
- `pytest-cache-files-*` Git 추적: 없음
- DB 저장 또는 SQLite repository 추가: 없음

## pyproject.toml 변경 없음 확인

- `pyproject.toml` 변경: 없음

## pytest cache warning 처리

`pytest-cache-files-*` permission warning은 비추적 임시 cache directory
permission warning이며 검증 결과에 영향이 없습니다.

## 다음 단계 제안

1. MS-05.15 변경분을 별도 승인 후 커밋합니다.
2. 커밋 전 staged diff에서 credential/token/header/raw body 원문이 없는지
   다시 확인합니다.
3. main 병합과 push는 별도 승인 후 진행합니다.
