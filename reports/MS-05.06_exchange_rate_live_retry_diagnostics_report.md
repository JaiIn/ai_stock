# MS-05.06 Exchange Rate Live Retry Diagnostics Report

## 수행 목적

MS-05.05에서 추가한 safe phase/status diagnostics를 적용하여
`GET /api/v1/exchange-rate` live smoke test를 승인된 범위에서 정확히 한 번
재시도하고, 민감정보나 raw response 없이 실패 지점을 확인했습니다.

## 생성 브랜치

- `codex/api/MS-05.06-exchange-rate-live-retry-diagnostics`

## 기준 커밋

- `fc26a29 feat(api): MS-05.05 add read-only live smoke diagnostics`

## 사전 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS, 112 tests
- `python -m pytest`: PASS, 134 tests
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

## Env key 존재 확인 결과

값을 출력하지 않고 아래 조건이 모두 충족됨을 확인했습니다.

- `TOSS_CLIENT_ID`: 존재
- `TOSS_CLIENT_SECRET`: 존재
- `ALLOW_LIVE_API=true`
- `ALLOW_REAL_ORDER=false`
- `DRY_RUN_ONLY=true`

## `.env.local` Git 제외 확인 결과

- 로컬 파일 존재
- `.gitignore` 적용
- Git tracked 아님
- Git status에 표시되지 않음
- 파일 내용 미출력

## 실제 호출 범위

- 허용: `POST /oauth2/token` 최대 1회
- 허용: `GET /api/v1/exchange-rate` 최대 1회
- smoke script 실행 횟수: 1회
- retry loop 및 자동 재시도: 없음
- 그 외 업무 API 호출: 없음

## OAuth token endpoint 호출 여부

- 호출: 1회
- 진단 결과가 `readonly_exchange_rate` phase에 도달했으므로 OAuth token 단계는
  성공한 것으로 확인됩니다.
- Token 원문은 출력하거나 저장하지 않았습니다.

## `GET /api/v1/exchange-rate` 호출 여부

- 호출: 1회
- 추가 재시도: 없음

## Live smoke test 결과

- success: `false`
- phase: `readonly_exchange_rate`
- endpoint: `GET /api/v1/exchange-rate`
- method: `GET`
- HTTP status code: `400`
- safe error code: `invalid-request`
- safe error type: `http_error`
- safe error message: `HTTP request failed.`

## Parsed response schema 요약

HTTP 400으로 종료되어 성공 응답 parsing은 수행되지 않았습니다.

- `baseCurrency`: 확인 불가
- `quoteCurrency`: 확인 불가
- `rate`: 확인 불가
- `validFrom`: 확인 불가
- `validUntil`: 확인 불가
- raw response body: 미저장

## 보안 및 금지사항 확인

- Access Token 원문 미출력·미저장
- Client ID/Client Secret 원문 미출력·미저장
- Authorization Bearer 원문 미출력·미저장
- request body 원문 미출력·미저장
- raw response body 전문 미저장
- accountSeq 요청·사용·저장 없음
- `GET /api/v1/exchange-rate` 외 실제 업무 API 호출 없음
- 실제 주문/계좌/자산/잔고/체결 호출 없음
- 실제 DB 저장 및 DB 파일 생성 없음
- `pyproject.toml` 변경 없음

## 사후 검증 결과

- `python -m compileall -q src tests`: PASS
- `python -m unittest discover -s tests`: PASS, 112 tests
- `python -m pytest`: PASS, 134 tests
- `python scripts/dev_check.py`: PASS
- `ruff check src tests`: PASS
- `git diff --check`: PASS

## 문제점 또는 확인 필요 사항

- HTTP 400과 safe code `invalid-request`가 확인되었습니다.
- Raw response를 저장하지 않는 정책 때문에 세부 서버 메시지는 수집하지 않았습니다.
- 공식 문서상 optional `dateTime`을 생략한 요청이 실제 환경에서 허용되는지,
  또는 별도 필수 형식이 있는지 read-only 공식 문서 재확인이 필요합니다.
- 추가 live 호출은 이번 단계에서 수행하지 않으며 별도 사용자 승인이 필요합니다.

## 다음 단계 제안

1. 실제 API를 호출하지 않고 공식 OpenAPI의 exchange-rate query 요구사항과
   오류 schema를 다시 확인합니다.
2. 필요한 경우 별도 Micro Stage에서 request parameter 정렬을 수행합니다.
3. 추가 live 재시도는 코드·문서 정렬 후 별도 승인을 받아야 합니다.
